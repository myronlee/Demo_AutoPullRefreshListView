<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ligang.demo_autopullrefreshlistview" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".TestActivity"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TestInterceptActivity"
            android:label="@string/title_activity_test_intercept" >

        </activity>
        <activity
            android:name=".TestOnStopActivity"
            android:label="@string/title_activity_test_on_stop" >
        </activity>
    </application>

</manifest>
